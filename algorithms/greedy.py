def greedy(origin, destination, doc_sim_func):    """    Tries to find a path between the origin    and destination page. Always travels to    the page with the highest similarity to    the destination page.    Parameters    ----------    origin : str    destination : str    doc_sim_func : function        Takes two wikipedia page titiles and returns        some measure of their similarity.    Returns    -------    None or list    """